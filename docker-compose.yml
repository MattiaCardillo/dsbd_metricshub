version: '3.8'
services:
  etldatapipeline:
    build:
      context: .
      dockerfile: ./etldatapipeline/Dockerfile
    command: 'python ./etldatapipeline/main.py'
    ports:
      - 5000:5000
    volumes:
      - .:/app

  prometheus:
    image: prom/prometheus
    ports:
      - 9090:9090
    volumes:
      - ./etldatapipeline/prometheus.yml:/etc/prometheus/prometheus.yml