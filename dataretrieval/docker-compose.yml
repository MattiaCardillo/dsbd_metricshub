version: '3.8'
services:
  dataretrieval:
    build:
      context: .
      dockerfile: Dockerfile
    command: 'python main.py'
    environment:
      - isDocker=true
    ports:
      - 5002:5002
    volumes:
      - .:/dataretrieval